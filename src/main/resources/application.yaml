server:
  port: 8080
  servlet:
    context-path: /bookstore

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs

spring:
  datasource:
    url: jdbc:mysql://root:CKvPJZEVTbBDegidMImXTFLUoAcCmRKv@mysql.railway.internal:3306/railway
    username: root
    password: CKvPJZEVTbBDegidMImXTFLUoAcCmRKv

  data:
    mongodb:
      uri: ${mongoDB_uri}
      auto-index-creation: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

jwt:
  signerKey:${signer_key}

cloudinary:
  cloud-name: dfinfmcan
  api-key: ${CLOUDINARY_KEY}
  api-secret: ${CLOUDINARY_SECRET} 

stripe:
  apiKey: ${STRIPE_KEY}
openai:
  api:
    key: ${OPENAI_API_KEY}

server:
  port: 8080
  servlet:
    context-path: /bookstore

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/bookstore_mysql"
    username: "root"
    password: "root"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


